[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arriadne"
version = "0.1.0"
description = "Production-ready memory system with vector search, graph traversal, and hybrid retrieval"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Mantes", email = "dev@nousresearch.com" },
]
keywords = ["memory", "semantic-search", "vector-search", "knowledge-graph", "ai-agent"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Database",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]

dependencies = [
    "faiss-cpu>=1.7.4",
    "numpy>=1.24.0",
    "datasketch>=1.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "mypy>=1.0.0",
    "ruff>=0.1.0",
]

[project.scripts]
ariadne = "arriadne.cli:main"

[project.urls]
Homepage = "https://github.com/mantes/arriadne"
Repository = "https://github.com/mantes/arriadne"
Changelog = "https://github.com/mantes/arriadne/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/arriadne"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.ruff]
target-version = "py310"
line-length = 100
